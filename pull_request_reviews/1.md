### Overall ✨

Strong, pragmatic Phase 1 HLD and System Architecture. The docs provide a clear modular-monolith vision and cover the main modules. Please incorporate the actionable items below to tighten scope for a fast MVP, improve implementation readiness, and ensure production operability.

### File: docs/HLD_Phase_1.md — General / Section-level suggestions:

1. **MVP Scope**: Add an explicit "MVP Scope" section (must-haves vs nice-to-haves). Example must-haves: OTP auth + JWT, watchlist CRUD, view company details with latest news+basic sentiment, admin manual news ingestion. This removes ambiguity and enables faster shipping.

2. **Non-goals**: Add a list clarifying what will not be done in Phase 1 (e.g., payment gateway, ML-based sentiment, push notifications). This prevents scope creep.

3. **User Stories & Acceptance Criteria**: Add this after Goals. Provide 5–8 concise user stories (e.g., user OTP login, add/remove watchlist item, admin adds company/news) with acceptance criteria.

4. **MVP API Surface**: Add a minimal "MVP API Surface" (concise list of endpoints) so frontend can parallelize work. Suggested endpoints: `/auth/otp/request`, `/auth/otp/verify`, `/auth/token/refresh`, `/users/me`, `/users/me/watchlist`, `/companies/{id}/news`, `/admin/news`.

5. **Minimal Data Model / Schema**: Add a logical section with suggested tables and key columns (users, subscriptions, companies, watchlists, news, events, sentiment, trends). Include indexing recommendations for read-heavy queries.

6. **Caching & Read Optimization**: Recommend Redis for watchlist caches and for dashboard queries. Suggest cache TTLs (5–30s) and invalidation strategy.

7. **Background jobs & ingestion**: Use a lightweight job queue for news ingestion and periodic sentiment/trend recomputations.

8. **Security specifics**: Add recommended OTP TTL (5–10 minutes), OTP rate limits (5/hr), JWT access token TTL (~15m), refresh token policy, and secrets management notes.

9. **Observability & Ops**: Detail required operational items: structured logs, tracing, metrics, error tracking, backups and restore drills.

10. **CI/CD & Deployment**: Add a short plan: GitHub Actions for lint/tests, Docker images, staging auto-deploy, manual production deploy, and simple MVP deploy targets.

11. **Testing**: Recommend unit tests, integration tests against a test DB, and contract tests.

12. **Phase 1 backlog**: Suggest a 4–5 sprint plan (Auth → Watchlist → News ingestion → Observability & CI → Hardening).

13. **Metrics to track**: KPIs such as signups/day, DAU, watchlists created, API error rate, etc.

### File-specific nit / wording suggestions:

- Replace "read-heavy service" with: "read-heavy, low-latency service — plan early for caching and denormalized reads".
- Reconcile Subscription design decision and clarify Sentiment/Trends computation.

### File: docs/System_Architecture.md — System and ops suggestions:

1. **API Gateway options**: Clarify Phase 1 options: lightweight external gateway or backend middleware. List mandatory responsibilities.

2. **Caching placement & strategy**: Placement of Redis, its use for job queues vs cache in production.

3. **Background workers & queue**: Add a short worker pool diagram/note detailing worker responsibilities, scaling guidance.

4. **Data layer scaling**: Read replicas plan, partitioning guidance, eventual search index.

5. **Security architecture**: Add token revocation, anti-abuse measures, and webhook signing for future integrations.

6. **Observability & SLOs**: Example SLOs for Phase 1 and SLIs to monitor.

7. **Deployment options**: MVP deployment options and steps to migrate to k8s.

8. **Microservice extraction triggers**: Metrics and feature triggers for extracting modules.

9. **API versioning & contract**: Recommend OpenAPI specs and adopting `/v1/` prefix.

### Inconsistencies to fix across both docs:

- Subscription handling and Sentiment/trends location inconsistencies.

### Small default values you can add for clarity (optional):

- OTP TTL: 5–10 minutes
- OTP rate limit: 5 requests/hour per identifier
- Access token TTL: 15 minutes
- Refresh token TTL: 7–30 days with rotation.

### Tone:

These comments are recommended edits rather than blockers; label each section in the docs as "Must-have" vs "Recommended" where appropriate to help prioritize.